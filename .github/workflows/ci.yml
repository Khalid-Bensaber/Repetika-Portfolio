name: ci-small-test

on:
  push:
    branches: [ testing ]
  pull_request:
    branches: [ testing ]

jobs:
  small-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - name: Sanity check
        run: |
          echo "CI triggered for $GITHUB_REPOSITORY @ $GITHUB_SHA"
          echo "Runner: $(uname -a)"
          echo "CI is working."

